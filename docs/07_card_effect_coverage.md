# カード特殊効果の網羅テスト基盤

## 目的

カード効果の実装漏れを、個別のバグ報告だけでなくテスト基盤側でも検出できるようにする。

このドキュメントでは、現在のテストが担保している効果群と、まだ簡略化または未対応として扱う効果を明示する。

コード上の一覧は `src/game/cardEffectCoverage.ts` に置き、`tests/game/cardEffectCoverage.test.ts` でカードデータとの差分を検出する。
全カード/全技が発動時に落ちないことは `tests/game/cardEffectResolution.test.ts` で検出する。

## カバー対象

| 領域 | 対象 | 主な検証 |
| --- | --- | --- |
| カードプール | 通常カード126枚、仮アイコン、未実装フラグ | カード投入数、カード種別、アイコン、未実装フラグなし |
| 対象選択 | 全マジック、全モンスター技 | 盤面上で少なくとも1つ合法対象を持つ |
| 特殊射程 | 射程2、射程3、貫通、直線、特殊射程 | ヤンバル、ピグミィ、ケントゥリアス、グングニエル、ロロ系 |
| リソース | コスト、山札切れ、HPドロー、ストーン、レベルアップ | HP減少時ストーン、山札切れペナルティ、投資ストーン返却 |
| 複数対象 | 二重対象、入れ替え、手札選択、山札カテゴリ選択 | 二重の盾、ワープ、シフトチェンジ、リフレッシュ、カードサーチ |
| ランダム | ランダムレベル、ランダムストーン、ランダム追加ダメージ | seed固定で再現可能な期待値 |
| 継続防御 | シールド、特殊防御、浄化時の残存/解除 | 女神の加護、竜の盾、スケープゴート、デスチェーン |
| 行動制限 | 特技封じ、挑発、石化/呪い、ダークホール | 行動候補とターン処理への反映 |
| 撃破/被ダメージ | 復活、反撃、退却、献身、怨念、仮死 | ダメージ処理と撃破処理の割り込み |
| CPU利用 | マジック、シールド、ウェイクアップ、移動後攻撃 | 判断理由ログと代表ケース |
| カード単位期待値 | 効果ズレが起きやすい個別カード | 公式効果文に対応する状態変化 |
| seed検証 | 100戦単位のオートプレイ | 例外、未解決プロンプト、長期戦、不審判断のartifact化 |

## テスト基盤

追加した `tests/game/cardEffectCoverage.test.ts` は、次を検出する。

- カードデータに新しいマジックカテゴリが増えたのに、カバー一覧へ追加されていない。
- モンスター技に新しい特殊traitが増えたのに、カバー一覧へ追加されていない。
- カバー領域に検証パスが紐付いていない。
- `SPD使用不可`、非売品、入手難度、形態名だけの注記など、戦闘挙動に関係しない情報が表示用注記へ混入していない。
- 簡略化/未対応一覧に、現在挙動、理由、次作業が書かれていない。

既存の `tests/game/rules.test.ts` は、具体的なルール挙動を担保する。今回の新規テストは、それらを横断するメタテストとして扱う。

`tests/game/cardEffectResolution.test.ts` は、次を検出する。

- 全マジックカードを実際に1回解決して、対象選択後に例外が出ない。
- 全モンスター技を実際に1回解決して、技発動後に例外が出ない。
- 対象列挙テストでは拾えない「対象は取れるが発動時に落ちる」不具合。

`tests/game/cardOfficialExpectations.test.ts` は、公式攻略データの効果文に対して、カード単位で状態変化を固定する。

現在追加済み:

- `card_027` パワーダウン
- `card_059` パワー２
- `card_062` 水晶の壁
- `card_063` どこでも
- `card_086` 呪縛
- `card_094` バーサクパワー
- `card_118` かまいたち
- `card_119` バイストーン
- `card_127` 癒しの光
- `card_130` 再生
- `card_149` 福音の鐘
- `card_150` スパルタス覚醒
- 特殊魔法/呪印魔法: `card_057`, `card_058`, `card_061`, `card_064`, `card_087`, `card_090`, `card_097`, `card_098`, `card_124`, `card_125`, `card_128`, `card_129`
- モンスター固有技/性格: `card_015`, `card_033`, `card_035`, `card_039`, `card_040`, `card_043`, `card_052`, `card_053`, `card_066`, `card_069`, `card_072`, `card_074`, `card_076`, `card_078`, `card_080`, `card_082`, `card_083`, `card_084`, `card_085`, `card_104`, `card_105`, `card_106`, `card_107`, `card_108`, `card_109`, `card_111`, `card_112`, `card_133`, `card_134`

## 公式攻略データと実装期待値の差分表

| 項目 | 状態 | 現在の扱い | 次作業 |
| --- | --- | --- | --- |
| 通常カード126枚 | 対応済み | 公式名、効果文、仮アイコン、実装フラグをカードデータに保持 | 個別期待値テストを継続追加 |
| 複数対象カード | 対応済み | 手動UIとCPUは候補を選択。未指定APIのみ決定的補完 | 新規カード追加時にsecondaryTargetテストを追加 |
| 手札/山札選択カード | 対応済み | 手動UIとCPUは候補を選択。リフレッシュは手札価値で捨て札を評価 | 手札上限/デッキ残量のケースを追加継続 |
| ランダム効果 | 対応済み | `GameState.randomSeed`で決定的解決し、結果ログと簡易エフェクトで表示 | 新規ランダム効果追加時に結果ログテストを追加 |
| CPU特殊効果判断 | 簡略化 | 公式AI再現ではなく、反撃/次ターン価値込みのヒューリスティック | 公式AI再現が必要なら別モードで設計 |
| 仮アイコン | 簡略化 | 攻略サイト由来の画像をテスト用として表示 | 自作画像に差し替え |
| スペシャルカード | 方針決定 | 通常カード126枚で戦闘後報酬へ進む。スペシャルカードは後段で別導入を再判断 | 報酬ループ完成後に導入可否を再レビュー |

## 未対応/簡略化カード効果一覧

| ID | 状態 | 内容 | 現在の扱い | 次作業 |
| --- | --- | --- | --- | --- |
| `super_cards_pending` | 未対応 | スペシャルカード | 通常カード126枚のみを投入し、戦闘後報酬へ進む | 報酬ループ完成後に導入可否を再レビュー |
| `per_card_assertion_gap` | 未対応 | 全カード個別の公式一致テスト | 効果群代表テスト、全カード合法対象テスト、全カード解決スモークテスト、個別期待値テストを併用 | 特殊効果カードから1枚ずつ期待値テストを増やす |
| `cpu_magic_heuristic` | 簡略化 | CPU特殊効果判断 | 複数対象、手札選択、サーチカテゴリ、次ターン反撃/レベルアップ価値まで浅い評価で判断 | 公式AI再現が必要なら別モードで設計 |
| `temporary_original_icons` | 簡略化 | 攻略サイト由来の仮アイコン | テスト用画像として表示 | 自作画像へ差し替え、命名規則とチェックを追加 |

## 運用

カード効果を追加または修正した場合は、次を同じコミットで更新する。

1. `src/game/cardData.ts`
2. `src/game/rules.ts` または `src/game/cpuAi.ts`
3. 具体挙動のテスト
4. `src/game/cardEffectCoverage.ts`
5. 必要ならこのドキュメント

warning seedが出た場合は、まず `docs/06_warning_seed_review.md` と同じ粒度で分類する。カード効果漏れならルールテストへ、CPU判断品質ならCPUテストまたはオートプレイ検証へ戻す。
